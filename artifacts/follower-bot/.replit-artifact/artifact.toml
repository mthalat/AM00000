kind = "web"
previewPath = "/"
title = "بوت تبادل المتابعين الذكي"
version = "1.0.0"
id = "artifacts/follower-bot"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 21122

[services.development]
run = "pnpm --filter @workspace/follower-bot run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/follower-bot", "run", "build" ]
publicDir = "artifacts/follower-bot/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "21122"
BASE_PATH = "/"
